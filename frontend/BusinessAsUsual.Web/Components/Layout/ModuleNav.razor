<MudTabs>
    <MudTabPanel Text="HR" OnClick="@(() => OnModuleSelected.InvokeAsync("HR"))" />
    <MudTabPanel Text="Orders" OnClick="@(() => OnModuleSelected.InvokeAsync("Orders"))" />
    <MudTabPanel Text="Inventory" OnClick="@(() => OnModuleSelected.InvokeAsync("Inventory"))" />
    <MudTabPanel Text="Accounting" OnClick="@(() => OnModuleSelected.InvokeAsync("Accounting"))" />
    <MudTabPanel Text="Timekeeping" OnClick="@(() => OnModuleSelected.InvokeAsync("Timekeeping"))" />
</MudTabs>

@code {
    [Parameter] public EventCallback<string> OnModuleSelected { get; set; }
}