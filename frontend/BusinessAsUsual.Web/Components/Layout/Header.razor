@code {
    [Parameter] public EventCallback OnToggleSidebar { get; set; }
    [Parameter] public bool IsDark { get; set; }

    private async Task ToggleSidebar()
    {
        if (OnToggleSidebar.HasDelegate)
            await OnToggleSidebar.InvokeAsync();
    }
}

<header class="sticky-top bg-primary text-white shadow px-3 py-2 z-3">
    <div class="d-flex justify-content-between align-items-center">
        <div class="d-flex align-items-center gap-2">
            <img src="/images/logo.png" alt="BAU Icon" width="32" height="32" loading="lazy" />
            <h1 class="h5 mb-0">Business As Usual</h1>
        </div>
        <ThemeToggle />
    </div>
</header>